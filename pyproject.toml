[project]
name = "worldsmith"
version = "0.1.0"
description = "Gate C feasibility prototype for Project Worldsmith"
requires-python = ">=3.12"

[project.scripts]
worldsmith = "worldsmith.cli:run"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
